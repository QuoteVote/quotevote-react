import React from 'react'

import ProfileHeader from './ProfileHeader'

export default {
  component: ProfileHeader,
  title: 'Profile Header',
}

export const ProfileHeaderComponent = () => <ProfileHeader />
